node_modules/

# Real private keys live in both of these.
.env
VANITY.txt

# vanitygen: AGPL-3.0 upstream, cloned and patched at build time by
# scripts/build-vanitygen.sh. Only vendor/patches/ is tracked.
vendor/vanitygen/

# Build output
cuda/gpu-vanity
cuda/gpu-vanity64
cuda/gpu-vanity-s[123]
cuda/gpu-hf[123]
cuda/*.o
